<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:background="@drawable/borderline">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/card_comment_profileImg"
        android:layout_width="70dp"
        android:layout_height="70dp"
        app:civ_border_color="#FFFFFF"
        app:civ_border_width="2dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/profile_pic" />

    <TextView
        android:id="@+id/card_comment_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto"
        android:text="@string/name"
        android:textColor="@color/mainFontColor"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_toEndOf="@id/card_comment_profileImg"
        android:layout_marginStart="20dp"/>

    <TextView
        android:id="@+id/card_comment_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto"
        android:text="date"
        android:textColor="@color/mainFontColor"
        android:textSize="14sp"
        android:layout_below="@id/card_comment_name"
        android:layout_toEndOf="@id/card_comment_profileImg"
        android:layout_marginStart="20dp"/>

    <TextView
        android:id="@+id/card_comment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto"
        android:text="comment"
        android:textColor="@color/mainFontColor"
        android:textSize="18sp"
        android:layout_below="@id/card_comment_date"
        android:layout_toEndOf="@id/card_comment_profileImg"
        android:layout_marginStart="20dp"/>

</RelativeLayout>